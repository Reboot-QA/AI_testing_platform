# 接口自动化重构 · 进度与计划总览

> 最后更新：2026-07-14（场景加步骤增强 C3：裸HTTP/从接口/cURL 导入 成型待合）· 单一事实来源，随进度滚动更新。
> 两条主线：**后端 P 系列**（Apifox 式分层重构，已基本完成）+ **前端 F 系列**（UI 仿 Apifox 化，进行中）。
> 分支流：本地分支 → PR 到 `test` → `test` 合 `main`。前端 PR 只提交源码+package，不含 node_modules/dist（部署从源码 npm install+build）。

---

## 一、后端 P 系列（Apifox 式分层平台）

将旧「接口自动化」重构为 Apifox 式分层模型：接口(Endpoint) / 环境·变量 / 接口用例 / 数据模型 / 脚本库+全局参数 / 场景编排 / 执行引擎 / 导入 / 调度。前缀 `apifox_`，锚定现有「部门+项目」权限模型，与旧模块灰度并存。

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P1** | 工作台入口 + 项目工作区（顶部 6 tab） | ✅ 已合 main |
| **P2.1** | 接口管理（文件夹树 + 接口 Endpoint） | ✅ 已合 main |
| **P2.2** | 环境·变量（远程团队值 + 每人本地值分层） | ✅ 已合 main |
| **P2.3** | 单接口用例（拆旧 `__meta` blob；断言/提取/前后置引用脚本库/数据驱动） | ✅ 已合 main |
| **P2.4** | 数据模型（JSON Schema） | ✅ 已合 main |
| **P2.5** | 脚本库（项目级）+ 全局参数 | ✅ 已合 main |
| **P3** | 场景编排（步骤：用例/等待/子场景，防循环引用） | ✅ 已合 main |
| **P4** | SSE 执行引擎 + 测试报告（变量流转/断言/提取/脚本/数据驱动） | ✅ 已合 main |
| **P5.1** | OpenAPI 导入（URL 拉取 / 粘贴 JSON，dogfooding 导入自身 173 接口） | ✅ 已合 main（PR #23/24） |
| **P5.2** | 定时调度（daily/weekly/interval，复用旧调度线程；定时模式读写远程值） | ✅ 已合 main（PR #25/26） |
| **P6a** | 软下线旧模块：**跳过迁移**(dev 旧模块空+迁移有损)；隐藏 4 个旧菜单 + 旧路由重定向到 /apifox（可逆，零后端改动） | 🔄 PR #37 待合 |
| **P6b** | 硬删除旧代码(ApiAutomation.vue/api_automation.py/ApiCaseEditor.vue+旧API封装)，保留 4 个共享服务函数 | ⬜ 后续独立 PR（软下线稳定后） |

---

## 二、前端 F 系列（UI 仿 Apifox 化）

参考项目 `D:\workspace\Apifox-UI-main`（React/Next.js，纯 UI demo 无后端）。**决策**：纯 Vue 重写（不嵌 React）/ 渐进增强（保留现路由 tab）/ 先于 P6 / 代码编辑器用 Monaco。数据层复用现有后端，契约不变。

| 阶段 | 内容 | 状态 |
|---|---|---|
| **F0** | 基建地基：Monaco 懒加载(本地 worker 无 CDN) + design token(Apifox 色板+品牌色) + 通用组件 CodeEditor/JsonView/MethodTag + composables 目录 | ✅ 已合 main（PR #27/28） |
| **F1** | 叶子组件接入：接口 Body/数据模型/脚本内容换 Monaco；报告响应体换可折叠 JSON 树；方法标签统一 Apifox 配色 | ✅ 已合 main（PR #29/30） |
| **F2** | 视觉皮肤统一：19 个组件散落 hex 收敛到 design token（零视觉回归的维护性基建）；Workbench 卡片加品牌强调条 | ✅ 已合 main（PR #31/32） |
| **F3** | 接口管理树增强：搜索框 + 右键菜单(新建/重命名/复制/删除) + 拖拽移动排序；后端 tree/reorder 端点 + 文件夹环形防护 | ✅ 已合 main（PR #33/34） |
| **F4** | 数据模型递归 Schema 编辑器：可视化字段树(任意嵌套) + Monaco 源码双视图，往返无损 | ✅ 已合 main |
| **F5** | 回收站（软删除/恢复）— 可选新增，需后端软删除支持 | ⬜ 可选/后置 |
| **F6** | 多标签工作台外壳（同时开多接口/用例 + 三栏可拖拽）— 激进项 | ⬜ 明确不做/未来 |

**顺带清理**（相关阶段内做）：拆超限的 `utils/apiCaseConfig.js`(1292 行)、`ProjectSettings.vue`(320 行)。

> **P6 结局**：软下线（P6a）曾合入，后按用户决策**还原为新旧并存**（旧模块与新 apifox 工作台各自独立、跳过数据迁移）；P6b 硬删除**不做**。

---

## 二·六、需求增强系列（用户四需求，均纯前端 / 已合 main）

> 在 F 系列基础上，用户提出四个体验需求，参照 Apifox。全部纯前端、零后端改动。

| 需求 | 内容 | 关键实现 | 状态 |
|---|---|---|---|
| **需求4** | 接口管理 Apifox 化 | 接口内多 tab（调试直接发送/文档预览/内嵌测试用例，不做 Mock）+ 空态卡片 + 统一「+新建」下拉 + 环境「前置 URL 分区」（多命名服务，接口按名选服务）+ `components/schemas` 导入为数据模型（anyOf/oneOf 取非 null 分支、$ref 递归展开） | ✅ 已合 main |
| **需求2** | 场景步骤拖拽编排 + 步骤详情 | 步骤区改「左拖拽列表 + 右步骤详情」；用例步骤详情内嵌 CaseEditor 直接改共享用例 | ✅ 已合 main |
| **需求3** | 单接口用例复用接口树 | 抽 `ApiTreePanel`（N1，ApiManage 368→172 行）；AutoTests「单接口用例」改用 `ApiTreePanel + ApiCasesPanel`（N2，273→62 行）——建模块=建接口文件夹、新增=新建接口，与接口管理同源 | ✅ 已合 main（PR #61/#62） |
| **需求1** | 工作台首页丰富 | 概览统计条（前端聚合）+ 快捷入口（新建/继续上次）+ 最近访问（localStorage）+ 全部项目搜索；抽 `WorkbenchStats`/`ProjectCard`/`useRecentProjects` | ✅ 已合 main（PR #63） |

**验收**：并入 `接口自动化v2_验收测试用例.md` v4 的 §17（需求4）/§18（需求2）/§19（需求3）/§20（需求1）。

---

## 二·七、Apifox 对齐系列（差异清单驱动，见 `Apifox差异清单与后续规划.md`）

> 原则：工作台等「Apifox 之外」以 PRD 为准，内部功能以真实 Apifox 为准。逐切片推进。

| 切片 | 内容 | 状态 |
|---|---|---|
| 工作台仪表盘 | 5 统计磁贴 + 我的项目 + 正在运行 + 最近报告；新增只读聚合接口 `GET /apifox/workbench/overview`（分层、无新表） | ✅ 已合 main（#65/66） |
| P0 工作区外壳 | 顶部项目切换下拉 + 统一环境切换（环境提 Pinia，调试/用例/场景共享） | ✅ 已合 main（#67/68） |
| P0.5 全局参数归位 | 全局参数从项目设置移入环境面板（对齐 Apifox §6） | ✅ 已合 main（#69/70） |
| P0.5 断言操作符 | status_code/json_path/header 支持 eq/neq/contains/gt/gte/lt/lte/regex/exists；加 operator 列+迁移 | ✅ 已合 main（#71/72） |
| P0 收尾 | 报告列表环境列 + 接口树数据模型段（可搜索/跳转自动选中） | ✅ 已合 main（#73/74） |
| P1 接口级处理器 | 断言/提取/前后置脚本挂接口，合并叠加（前置接口→用例、后置用例→接口、并集），调试执行；新增 3 表 | ✅ 已合 main（#75/76） |
| P1 契约校验 | 接口绑响应模型 + fastjsonschema 校验响应，contract_strict 可配，调试/报告展示；新增 3 列 + 依赖 | ✅ 已合 main（#77/78） |
| P2 项目设置 | 基本信息（改名/描述）+ 删除项目（级联清空 23 张 apifox 表，FK 安全）；无新表 | ✅ 已合 main（#79/80） |
| P1 流程控制·S1 步骤树+分组 | 场景步骤扁平链→可嵌套树（邻接表 parent_step_id+config 列）；交付「分组(group)」容器 + depth 列；同引擎按树执行；顺带启用 pytest 基建、停止跟踪 dist | ✅ 已合 main（#81/82） |
| P1 流程控制·S2 条件分支 | if/else（if 容器 config 存 condition、else 嵌套子容器、elif=else 内嵌 if）；复用断言操作符引擎求值 + {{var}} 插值；未命中分支不落库；零新表零迁移 | ✅ 已合 main（#83/84） |
| P1 流程控制·S3 循环 | loop（count 固定次数/list 遍历 JSON 数组变量注入 item+index/while 条件+max_iterations）；MAX_LOOP_ITERATIONS=1000 硬兜底防死循环；循环变量按层级作用域（嵌套不串）；零新表零迁移 | ✅ 已合 main（#85/86） |
| run_engine 拆分重构 | 偿还超软限技术债：`run_engine.py` 565→329 行，按职责下沉为 facade + `variables`/`operators`/`flow_control`/`request_builder` 四子模块，依赖单向无环；行为零变更、调用方零改动（facade re-export）；补 61 个纯函数单测（全量 133 passed） | ✅ 已合 main（#87/88） |
| P1 流程控制·break/continue | 循环体内 break(跳出)/continue(跳过本轮)：新叶子步骤 raise 私有信号异常穿透嵌套生成器、最近 `_run_loop_block` 捕获；嵌套只跳最内层、if 内穿透；try/finally 循环变量在 break 路径仍恢复；沿祖先链 in_loop 校验拒绝循环外；零新表零迁移 | ✅ 已合 main（#89/90） |
| P2 测试套件 | 用例/场景成组批量回归：新 `apifox_suites`/`apifox_suite_items` 两表 + 三层 CRUD；执行走**父运行+子运行**（`ApifoxRun` 加 `parent_run_id`，每项独立 fresh runtime 互不串变量，单项失败隔离 + try/finally 兜底防父运行卡 running）；SSE `POST /suites/{sid}/run/stream`；报告两级下钻（套件→子项→步骤）；定时调度 target 扩 suite；前端 SuitePanel 子页 + SuiteRunProgress。20 单测（全量 165 passed，含评审整改回归用例） | ✅ 已合 main（#91/92） |
| P2 数据模型 $ref 绑定+引用计数 | 模型间用 `{"$ref":"#/models/<名>"}` 互引用（名唯一）；新 `schema_ref` 模块递归内联跨模型引用（环/未知/超深/DB异常降级），契约校验前解析让含 $ref 的响应模型真正生效；引用计数（接口响应契约按 id + 模型 $ref 按名）+ 删除拦截 + 被引用禁改名 + 保存时环/未知引用校验；前端字段树加「引用模型」类型 + 引用数徽标。零新表零迁移。22 单测（全量 187 passed，含评审整改回归用例） | ✅ 已合 main（#93/94） |
| P2 项目级静态数据集 | 可复用测试数据表提升为项目级资源：新 `apifox_datasets`/`apifox_dataset_rows` 两表（行级表，利于未来单行并发编辑）+ 三层 CRUD；用例数据驱动加 `source(inline|dataset)`+`dataset_id`，`data_drive_rows(case, db)` 按数据集行迭代（校验属本项目、跨项目忽略、非法id兜底、向后兼容）；引用计数（扫用例 data_drive）+ 删除保护；前端「数据集」子页(列/行编辑) + 数据驱动数据源切换。零迁移。12 单测（全量 198 passed，评审无阻塞） | ✅ 已合 main（#95/96） |
| P2 数据库操作步骤 | 场景新增「数据库操作」步骤（造数/清理/取数，可放循环体内）：新 `apifox_environment_databases` 表（环境级连接，dev/prod 各连各库，密码只写不回显）+ 三层 CRUD + 连接测试；`db_executor` 临时引擎 exec_driver_sql 执行任意 SQL(写操作提交)、超时、不抛异常+日志；`run_service._run_db_step` 解析连接(须属当前环境、connection_id 类型校验兜底)→{{var}}插值→执行→首行列提取变量(复用作用域落库)→落 run step；前端环境 DB 连接管理 + 场景 DB 步骤编辑(Monaco SQL+提取)。仅 MySQL，零迁移。16 单测（全量 215 passed，评审阻塞已整改） | ✅ 已合 main（#97/98） |
| 数据模型对齐 C1+C2（Apifox 对齐·纯前端）| **C1 字段树可编 JSON Schema 高级约束**（enum/pattern/min-max/minLength/format/default/example/nullable/uniqueItems）+ **源码↔可视化往返无损**（`useJsonSchema` 加 extra 透传袋，连未知手写关键字/空串默认值/布尔标志/数值枚举都保类型无损）；新 `SchemaConstraintsEditor` 弹出层按类型编辑。**C2 删除/改名保护前端 UX**（头部「被 N 处引用」徽标、删除按 ref_count 预警、后端 400 优雅提示；后端拦截已存 #93/94）。round-trip 实测 + 评审 4 类型 bug 已整改 | ✅ 已合 main（#103/104）。**前端 UI 待 dev 眼验** |
| 场景加步骤增强 C3（Apifox 对齐）| 场景新增**裸 HTTP 请求步骤**（type=http，config 自带 method/path/request_spec/断言/提取；`engine.execute_http_request` 鸭子类型假 endpoint 复用 build_request+断言+提取）+ **从接口导入**（拉接口 request_spec 建裸步骤）+ **从 cURL 导入**（新 `curlParser` 解析）；前端复用 ApiEndpointEditor(show-meta)+AssertionsEditor/ExtractsEditor。6 单测（全量 233 passed）。零新表零迁移 | 🔄 分支 `feat/apifox-http-step`，待 PR。**前端 UI 待 dev 眼验** |
| **下一步** | P-B 场景/脚本未保存保护；P-C 剩余：场景运行配置面板(绑数据集/循环次数)；G1 实体扩展 / G2 旧功能下线（见二·八） | 🔜 待定 |

**未来路径（优先级）**：P0 剩余纯前端小补 → P1 三大结构性差距（① 接口级处理器+契约校验 ② 场景流程控制 ③ 测试套件/静态数据集，均需后端，作主线里程碑）→ P2/P3 详见路线图。

---

## 二·八、待纳入规划（用户新增，未排期）

> 用户 2026-07-13 补充，暂记录不启动，后续择机独立立项。

| 项 | 内容 | 关注点 / 初步方向 | 状态 |
|---|---|---|---|
| **G1 多人协同并发编辑** | 同项目不同成员同时编辑**同一接口或其内部任一内容**，避免 last-write-wins 互相覆盖 | 对齐 `concurrency.md`。**阶段一·保存冲突检测已合 main（#99/100）**：case/scenario 加 `version` 乐观锁，保存回传 `expected_version`，服务端 **DB 级原子 CAS**（`versioning.bump_version`）不一致→409，前端弹「加载最新/覆盖保存」；未传版本向后兼容。**endpoint 也已接乐观锁**（多 tab 铺底，见下）。→ script/schema/dataset 可同范式扩；**实时协同(WebSocket/CRDT)后置** | ✅ 阶段一已合 main（#99/100）；endpoint 随多 tab 切片 |
| **接口管理多标签 + 关闭未保存拦截**（需求2主 + 需求1） | 接口管理支持**同时打开多个接口(tab)**（对齐 Apifox）；关闭有未保存改动的接口 tab 时**强制弹窗**（保存并关闭/不保存关闭/取消）+ beforeunload 兜底 | 借鉴 `Apifox-UI-main` 的 ApiTab 改 Vue：新 `stores/apiTabs.js`（Pinia 存 tab，跨主 tab 存活、in-flight 去重、saving 按 tab 隔离），每 tab 独立 form/子tab/version + **初始快照深比较判 dirty**；endpoint 补乐观锁；`confirmCloseDirty` 关闭确认。endpoint 4 单测（全量 231 passed） | ✅ 已合 main（#101/102），**dev 基本功能已验收** |
| **下一步（承上）** | P-B：场景/脚本也纳入未保存保护（需求1 覆盖三者）；P-C：数据模型、自动化测试子模块对齐 Apifox（需求2 后半） | 待排期 | 🔜 |
| **G2 旧功能下线与技术债总清理** | 优化全部完成后，成体系地移除旧「接口自动化」冗余功能与陈旧代码、并做上帝文件优化与重新封装 | 统一立**一份下线总计划**：①盘点新旧功能对照与用户迁移路径 ②删旧模块（`ApiAutomation.vue`/`api_automation.py`/`ApiCaseEditor.vue`+旧 API 封装，保留共享服务函数）③重构真·上帝文件 `utils/apiCaseConfig.js`(1292 行，新旧共用需回归旧模块) ④收紧擦边文件（`run_service.py` 506 行拆 `suite_run.py`、`ProjectSettings.vue` 等）。**前置条件**：新平台功能完备且验收通过 | ⬜ 待排期（收尾阶段） |

---

## 二·五、代码质量体检（2026-07-09，用户决定"暂不动，记录现状"）
新 apifox 代码**无硬性上帝文件**（后端无 >600 行、前端无 .vue >400 行、无 JS >400 行）。以下为擦边/遗留，**待后续择机收紧**（非阻塞）：
- ~~`services/apifox/run_engine.py` 410 行（超软限400）~~ → **已拆分（#87/88）**：facade + `variables`/`operators`/`flow_control`/`request_builder` 四子模块，329 行进软限，各子模块 ≤104 行。
- `services/apifox/run_engine.py` 的编排层 `run_service.py` 506 行（超软限400，随套件父+子运行编排增长）→ 后续可把 `iter_suite_run` 一族下沉为 `suite_run.py` 子模块（复用 `iter_case_run/iter_scenario_run`）。
- `views/apifox/sections/ProjectSettings.vue` 310 行（超软限300）→ 可拆"脚本库/全局参数"两子组件。
- `views/apifox/sections/ApiManage.vue` 302 行（刚过软限，基本无碍）。
- **`utils/apiCaseConfig.js` 1292 行**：新旧共用的真·上帝文件（JS 上限3倍），legacy 遗留，拆分会动旧模块共用面 → 需回归旧模块，单独排期。

## 三、其他交付物 / 待办

- **验收测试用例**：`接口自动化v2_验收测试用例.md`（v4，覆盖 P1–P5.2 + F0–F4 + 需求 1/2/3/4 + 新旧并存）。全部走完后由用户做 dev 环境整体验收。
- **记录的 TODO**：全局调度中心（跨项目）、失败通知/重试、全局参数 body 位置、契约(schema)校验、SSE 中断、Swagger2.0/Postman/cURL 导入、导入自动同步。

## 四、部署机制（备忘）

| 环境 | 方式 | 说明 |
|---|---|---|
| dev | Vite dev server（`http://43.160.226.39:5173`，演示 admin/admin123） | 读源码热更新 |
| 生产 | Docker（`frontend/Dockerfile`：`npm ci`+`npm run build`） | 从源码重建 dist |
| 更新 | `update.sh`（git pull + 重新构建） | — |

→ `node_modules`/`dist` 无需进 git，部署自动装/构建。仓库现有的它们是历史遗留（暂不清理）。
